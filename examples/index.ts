import Canvas2DApplication from "../src/core/Canvas2DApplication";

const canvas: HTMLCanvasElement = document.getElementById('canvas') as HTMLCanvasElement;

const app = new Canvas2DApplication(canvas);